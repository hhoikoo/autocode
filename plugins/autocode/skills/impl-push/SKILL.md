---
name: impl-push
description: Commit local changes via git-commit and open a PR via pr-create; in a unit worktree also appends the epic PROGRESS rollup and advances the sub-issue to in-review. Use when a unit's implementation is ready to push.
---

Read through @~/.autocode/autocode/impl/skills/impl-push/SKILL.md and execute actions according to the instructions in the file. `$ARGUMENTS` is forwarded.
