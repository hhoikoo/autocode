---
name: design-plan-push
description: Open a lightweight PR for a tracked design doc. Ensures the design worktree/branch, then delegates to git-create-branch (if needed), git-commit, and pr-create --lightweight with the canonical design-PR body.
---

Read through @~/.autocode/autocode/design/skills/design-plan-push/SKILL.md and execute actions according to the instructions in the file. `$ARGUMENTS` is forwarded.
