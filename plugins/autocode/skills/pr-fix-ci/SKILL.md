---
name: pr-fix-ci
description: Diagnose failing CI checks on a PR and apply minimal fixes. Reads the repo's verify command from the build convention.
---

Read through @~/.autocode/autocode/pr/skills/pr-fix-ci/SKILL.md and execute actions according to the instructions in the file. `$ARGUMENTS` is forwarded.
