---
name: pr-review
description: Triage PR review comments (confidence-weighted), apply fixes, reply, and resolve threads via the configured git-host provider.
---

Read through @~/.autocode/autocode/pr/skills/pr-review/SKILL.md and execute actions according to the instructions in the file. `$ARGUMENTS` is forwarded.
