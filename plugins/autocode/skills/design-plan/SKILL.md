---
name: design-plan
description: Plan a non-trivial implementation. Sketches a rough plan, identifies gaps, dispatches researchers in parallel, then writes a sourced plan to .autocode/design/ (or a temp dir with --temp).
---

Read through @~/.autocode/autocode/design/skills/design-plan/SKILL.md and execute actions according to the instructions in the file. `$ARGUMENTS` is forwarded.
