# issue/

Issue lifecycle skills (`issue-view`, `issue-create`). Thin wrappers over `provider/run.sh issue-tracker <feature>`; never call `gh` or `jira` directly. The issue-types convention instructions live at `autocode/_config/conventions/issue-types.md`; the per-repo scaffold lands at `$AUTOCODE_CONFIG_DIR/conventions/issue-types.md`.
